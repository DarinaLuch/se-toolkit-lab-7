# LMS Telegram Bot

Telegram bot for accessing LMS analytics.

## Development

1. Install dependencies: `uv sync`
2. Test commands: `uv run bot.py --test "/start"`
3. Run bot: `uv run bot.py`

## Configuration

Copy `.env.bot.example` to `.env.bot.secret` and fill in your credentials.
